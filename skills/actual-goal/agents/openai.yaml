interface:
  display_name: "Actual Goal"
  short_description: "Keep work aligned with the real objective"
  default_prompt: "Use $actual-goal to rewrite this prompt so its objective, constraints, evidence, uncertainty, and verification outrank proxy cues."
policy:
  allow_implicit_invocation: false
